export const positiveTest: { query:string } = {
    query: 'khavyhana',
  };

export const negativeTest:{query:string, errorMessage: string}={
    query:'non-exist username',
    errorMessage: 'No user found with that name'
}